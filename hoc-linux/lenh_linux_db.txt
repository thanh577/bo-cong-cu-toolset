ls|Liệt kê tệp và thư mục|-a (hiện tệp ẩn);; -l (chi tiết);; -lh (chi tiết + dung lượng dễ đọc);; -lt (sắp theo ngày);; -R (đệ quy);; -S (sắp theo kích thước)
cd|Di chuyển giữa thư mục|.. (lùi 1 cấp);; ~ (về home);; - (quay lại thư mục trước);; /duong/dan (đường dẫn tuyệt đối)
pwd|In đường dẫn thư mục hiện tại|(không có option phổ biến)
mkdir|Tạo thư mục mới|-p (tạo cả cây thư mục lồng nhau);; -m 755 (đặt quyền ngay khi tạo)
rm|Xóa tệp vĩnh viễn không có thùng rác|-r (xóa thư mục);; -i (hỏi xác nhận từng cái);; -f (ép buộc không hỏi);; -v (hiện tên từng tệp bị xóa)
cp|Sao chép tệp hoặc thư mục|-r (sao chép thư mục);; -u (chỉ sao chép nếu mới hơn);; -v (hiện tiến trình);; -p (giữ nguyên quyền và ngày)
mv|Di chuyển hoặc đổi tên tệp|-i (hỏi nếu đích đã tồn tại);; -v (hiện tiến trình);; -n (không ghi đè)
cat|Hiển thị nội dung tệp|-n (kèm số dòng);; -A (hiện ký tự đặc biệt);; -s (gộp dòng trắng liên tiếp)
touch|Tạo tệp trống hoặc cập nhật thời gian|-a (cập nhật thời gian truy cập);; -m (cập nhật thời gian sửa đổi);; -t (đặt thời gian tùy chỉnh)
ln|Tạo liên kết đến tệp|-s (symbolic link);; -f (ghi đè nếu đã tồn tại);; -v (hiện tiến trình)
file|Nhận dạng loại tệp|-i (hiện MIME type);; -z (kiểm tra trong file nén)
tree|Hiển thị cây thư mục trực quan|-L 2 (giới hạn 2 cấp sâu);; -a (hiện tệp ẩn);; -d (chỉ hiện thư mục);; -h (kích thước dễ đọc)
grep|Tìm kiếm chuỗi văn bản trong tệp|-i (không phân biệt hoa/thường);; -r (đệ quy thư mục);; -n (hiện số dòng);; -v (dòng không khớp);; -l (chỉ tên file);; -c (đếm dòng khớp)
find|Tìm tệp theo điều kiện|-name (theo tên);; -type f/d (tệp/thư mục);; -size +100M (lớn hơn);; -mtime -7 (mới hơn 7 ngày);; -exec (thực thi lệnh với kết quả)
locate|Tìm tệp nhanh qua DB chỉ mục|-i (không phân biệt hoa/thường);; -c (đếm kết quả);; -l 5 (giới hạn 5 kết quả)
which|Tìm đường dẫn đầy đủ của lệnh|-a (hiện tất cả vị trí tìm thấy)
head|Xem N dòng đầu tệp|-n 20 (xem 20 dòng);; -c 100 (xem 100 bytes đầu)
tail|Xem N dòng cuối hoặc theo dõi log realtime|-n 20 (xem 20 dòng);; -f (theo dõi realtime);; -F (theo dõi kể cả khi file bị xoay)
less|Xem tệp dài có thể cuộn|Space (trang sau);; b (trang trước);; / (tìm kiếm);; q (thoát);; G (cuối file);; g (đầu file)
wc|Đếm dòng từ ký tự trong tệp|-l (chỉ đếm dòng);; -w (chỉ đếm từ);; -c (chỉ đếm bytes);; -m (đếm ký tự)
sort|Sắp xếp các dòng trong tệp|-r (ngược);; -n (theo số);; -u (bỏ trùng);; -k 2 (sắp theo cột 2);; -t : (dùng dấu : làm phân cách)
uniq|Lọc dòng trùng lặp liên tiếp|-c (đếm số lần xuất hiện);; -d (chỉ hiện dòng trùng);; -u (chỉ hiện dòng không trùng)
diff|So sánh nội dung hai tệp|-u (unified format dễ đọc);; -i (không phân biệt hoa/thường);; -w (bỏ qua khoảng trắng);; -r (so sánh thư mục)
tar|Nén hoặc giải nén tệp thư mục|-czf (nén .tar.gz);; -xzf (giải nén .tar.gz);; -tzf (xem nội dung);; -v (hiện chi tiết);; -C /dich (giải nén vào thư mục)
zip|Nén thành định dạng .zip|-r (nén thư mục);; -e (đặt mật khẩu);; -9 (nén tối đa);; -u (cập nhật file đã có)
unzip|Giải nén tệp .zip|-d /dich (giải nén vào thư mục);; -l (xem nội dung);; -o (ghi đè không hỏi);; -n (không ghi đè)
gzip|Nén tệp thành .gz thay thế file gốc|-k (giữ file gốc);; -d (giải nén);; -9 (nén tối đa);; -v (hiện tỉ lệ nén)
chmod|Thay đổi quyền truy cập tệp|+x (thêm quyền chạy);; 755 (chủ=rwx khác=r-x);; 644 (chủ=rw khác=r);; -R (đệ quy)
chown|Thay đổi chủ sở hữu tệp|-R (đệ quy);; user:group (đổi cả nhóm);; :group (chỉ đổi nhóm)
sudo|Chạy lệnh với quyền quản trị root|-i (vào phiên root);; -u user (chạy với tư cách user khác);; !! (chạy lại lệnh vừa rồi với sudo)
su|Chuyển sang người dùng khác|- (load đầy đủ môi trường user);; -c "lệnh" (chạy 1 lệnh rồi thoát)
passwd|Đổi mật khẩu tài khoản|-l (khóa tài khoản);; -u (mở khóa);; -d (xóa mật khẩu)
whoami|In tên người dùng đang đăng nhập|(không có option)
id|Xem UID GID và nhóm người dùng|-u (chỉ UID);; -g (chỉ GID);; -G (liệt kê tất cả nhóm);; -n (hiện tên thay vì số)
groups|Xem nhóm của người dùng|(nhập tên user để xem user khác)
top|Giám sát CPU RAM và tiến trình realtime|P (sắp xếp CPU);; M (sắp xếp RAM);; k (tắt tiến trình);; q (thoát);; 1 (hiện từng CPU core)
htop|Giám sát hệ thống nâng cao hơn top|F6 (sắp xếp);; F9 (tắt tiến trình);; F3 (tìm kiếm);; F10 (thoát)
ps|Liệt kê tiến trình đang chạy|aux (tất cả tiến trình);; -u user (theo user);; -p PID (theo PID);; --forest (dạng cây)
kill|Gửi tín hiệu tắt tiến trình theo PID|-9 (tắt buộc);; -15 (tắt nhẹ nhàng mặc định);; -l (liệt kê tất cả tín hiệu)
killall|Tắt tất cả tiến trình cùng tên|-9 (tắt buộc);; -i (hỏi xác nhận);; -u user (chỉ của user đó)
df|Xem dung lượng ổ đĩa các phân vùng|-h (dung lượng dễ đọc);; -i (xem inode);; -T (hiện loại filesystem)
du|Đo dung lượng thư mục hoặc tệp|-sh (tổng dung lượng dễ đọc);; -h (từng mục);; --max-depth=1 (chỉ 1 cấp);; --exclude (bỏ qua pattern)
free|Xem RAM và swap đang dùng|-h (dung lượng dễ đọc);; -s 2 (cập nhật mỗi 2 giây);; -t (hiện tổng cộng)
uname|Xem thông tin hệ điều hành và kernel|-a (toàn bộ thông tin);; -r (chỉ phiên bản kernel);; -m (kiến trúc CPU)
uptime|Xem thời gian máy đã chạy và tải hệ thống|-p (dạng dễ đọc: "up 2 days")
lsblk|Liệt kê ổ đĩa và phân vùng dạng cây|-f (hiện filesystem và UUID);; -d (chỉ thiết bị không phân vùng);; -o NAME,SIZE,TYPE (chọn cột)
mount|Gắn kết phân vùng vào hệ thống tệp|-t ext4 (chỉ định filesystem);; -o ro (chỉ đọc);; -a (mount tất cả trong /etc/fstab)
ping|Kiểm tra kết nối mạng đến địa chỉ|-c 4 (gửi đúng 4 gói rồi dừng);; -i 2 (gửi mỗi 2 giây);; -s 100 (kích thước gói)
curl|Gửi và nhận dữ liệu qua mạng|-O (tải file giữ tên gốc);; -o ten (tải đặt tên);; -I (chỉ xem header);; -X POST (HTTP method);; -d (gửi data);; -H (thêm header)
wget|Tải tệp từ internet về máy|-c (tiếp tục nếu bị ngắt);; -O ten (đặt tên file);; -q (im lặng);; -r (tải đệ quy)
ssh|Kết nối từ xa vào máy chủ Linux|-p 2222 (cổng tùy chỉnh);; -i key.pem (dùng file khóa);; -L (port forwarding);; -N (chỉ tunnel không chạy lệnh)
scp|Sao chép tệp qua kết nối SSH|-r (sao chép thư mục);; -P 2222 (cổng tùy chỉnh);; -i key.pem (dùng file khóa)
ip|Xem và quản lý địa chỉ IP mạng|addr show (xem IP);; route show (xem định tuyến);; link show (xem giao diện);; addr add/del (thêm xóa IP)
ss|Xem socket và kết nối mạng đang mở|-tuln (TCP+UDP đang lắng nghe);; -p (hiện tiến trình);; -s (thống kê tóm tắt)
apt|Quản lý phần mềm Ubuntu Debian|update (cập nhật danh sách);; upgrade (nâng cấp);; install (cài);; remove (gỡ);; autoremove (dọn gói thừa);; search (tìm);; show (xem thông tin)
snap|Quản lý gói Snap đa nền tảng|install (cài);; remove (gỡ);; list (liệt kê);; refresh (cập nhật);; --classic (bỏ sandbox)
dpkg|Cài hoặc gỡ tệp .deb trực tiếp|-i (cài .deb);; -r (gỡ);; -l (liệt kê đã cài);; -L (xem file của gói);; -s (xem thông tin gói)
nano|Soạn thảo văn bản đơn giản trong terminal|Ctrl+O (lưu);; Ctrl+X (thoát);; Ctrl+W (tìm kiếm);; Ctrl+K (cắt dòng);; Ctrl+U (dán)
vim|Soạn thảo văn bản mạnh mẽ trong terminal|i (vào insert mode);; Esc (về normal);; :w (lưu);; :q (thoát);; :wq (lưu+thoát);; :q! (thoát không lưu);; dd (xóa dòng)
man|Xem hướng dẫn đầy đủ của bất kỳ lệnh|-k từ_khóa (tìm theo từ khóa);; -f lệnh (mô tả ngắn);; số trang (vd: man 5 crontab)
clear|Xóa sạch màn hình terminal|Ctrl+L (phím tắt thay thế)
history|Xem lịch sử các lệnh đã gõ|-c (xóa lịch sử);; số (vd: history 20);; Ctrl+R (tìm kiếm nhanh);; !! (chạy lại lệnh cuối);; !số (chạy lệnh theo số)
echo|In chuỗi văn bản ra màn hình|-e (kích hoạt ký tự đặc biệt);; -n (không xuống dòng cuối);; > file (ghi vào file);; >> file (nối vào file)
date|Xem hoặc đặt ngày giờ hệ thống|'+%d/%m/%Y' (định dạng ngày);; '+%H:%M:%S' (định dạng giờ);; '+%s' (unix timestamp);; -d "ngày" (chuyển đổi)
cal|Hiển thị lịch tháng trong terminal|-y (cả năm);; -3 (tháng trước hiện tại sau);; tháng năm (vd: cal 6 2025)
bc|Máy tính dòng lệnh hỗ trợ số thập phân|-l (load thư viện toán học);; scale=2 (số chữ số thập phân)
alias|Tạo lệnh tắt cho lệnh dài hay dùng|unalias tên (xóa alias);; alias (xem tất cả alias hiện có)
crontab|Lên lịch chạy lệnh tự động|-e (chỉnh sửa);; -l (xem danh sách);; -r (xóa tất cả);; cú pháp: phút giờ ngày tháng thứ lệnh
systemctl|Quản lý dịch vụ hệ thống Ubuntu|start/stop/restart (bật tắt khởi động lại);; status (trạng thái);; enable/disable (tự khởi động);; list-units (liệt kê)
journalctl|Xem nhật ký hệ thống từ systemd|-f (theo dõi realtime);; -u tên (theo dịch vụ);; -p err (chỉ lỗi);; --since "1 hour ago" (lọc thời gian)
